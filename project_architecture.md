# 차세대 풍멜이 프로젝트 개발


## 개요
- 기존 풍멜이 관리 프로젝트 개선 진행 (legacy_project_architecture.md 참고)
- 기존 풍멜이 관리 프로젝트에 있는 core한 로직이 포함된 py파일을 최대한 활용한다.
- 기존 풍멜이 관리 프로젝트에서 사용하는 행동강령을 모두 추출하고, 본 프로젝트 부분별 프롬프트로 쪼개서 재구성한다. 
- 본 프로젝트 목표는 로직은 다시 짜는게 아닌, workflow화 하여 트래킹 가능한 형태로 재구성하는 것임.
- workflow 엔진 기반으로 체계적으로 노드화하여서 관리 + 트래킹 가능하게 ㄱㄱ
- workflow는 총 2개로 진행한다 (메일 체크 후 디코 발송, 오더시트/재고표 1시간씩 확인하여 발주상태값 갱신)
- 각 프롬프트는 prompt/ 하위에서 관리한다. (카테고리별 프롬프트는 하나의 파일이 아닌, 별도 파일로 관리)
- llm api는 기존 풍멜이 관리 openai api key를 똑같이 사용한다. gpt-nano 모델 사용 필수

## 시스템 결과물 검증사항
- 메일을 수신하면 카테고리중 하나로는 무조건 발송이 되어야한다. (유효 판단 안되는 경우 "보류" 채널로 발송)
- 관리 ui 필요함
    - workflow 노드 구성이 ui로 보여야함
    - workflow가 실행되었을때, 실행이력 및 상태도 보여야함
    - 서버 로그도 확인가능해야함 
    - 발주 데이터 조회 가능해아함. 히스토리 내역도 확인 가능해야함
    - rw,si 데이터 조회도 가능해야함. 서로 연결관계 있기 떄문에 동적으로 parent-child 페이지 linking 기능 있어야함 
- 검증 도구 필요
    - 이전 메일들 테스트 데이터도 따로 저장
    - 해당 메일 클릭해서 수동 workflow 실행할 수 있어야함
    - 수동실행하여, 노드별흐름 및 최종 결과도 확인 가능해야함

## 공통 처리
- 디스코드 메시지는 Db화 하여 메시지 발송. column 중 하나는 gmail id (추후 메시지 수정/삭제 추적 필요를 위함)
- 이 문서에는 동작만 정의함. 필요한 데이터 모델처리는 codex가 진행 해도됨.

## 메일 체크 workflow 흐름

### 1. 1분마다 이메일 확인
- 기존 py 파일 참고

### 2. 이메일 유효 조건 확인
- 기존 py 파일 참고

### 3. 이메일 내용 추출
- 기존 Py 파일 참고
- 이미지 ocr하여 텍스트 추출
- 첨부파일 까서 텍스트 추출
- 스레드 히스토리까지 추출

### 4. ai로 카테고리 분류
- [카테고리 분류 프롬프트](mail_category_classify_prompt.md)을 따라서 카테고리로 분류한다.
- 카테고리는 아래와 같음
    - 발주
    - 오더
    - 샘플자료견젹
    - 풍림자료요청
    - 사내업무
    - 해외업무
    - 보류
- 카테고리들을 별도 데이터 규격으로 만든다.
- ai를 통해서 structured output 형태로 무조건 카테고리 중 하나로 정하게 된다.

### 5. 카테고리별 처리

#### 5.1. 발주처리
- 디코 채널은 2개로 구분됨.
    - 발주 (디코 카테고리)
        - 발주 (디코 채널)
        - 오더시트 (디코 채널)

##### 5.1.1. 발주 스펙 확인
- 발주 스펙 확인 총 4가지 종류가 있음. 4가지에 대해서는 해당되면 진행하여 데이터 가져오면 돼. (4가지는 모두 풍멜이 관리 프로젝트에 있음. Py 파일 등 로직 가져오면 됨)
- 1 메일 본문 확인: 실제 작성문에서 품목·정확한 grade·수량·납기·납품처 확인
- 2 첨부 발주서 확인: PDF·Excel·이미지의 표와 PO를 판독하고 필요 시 OCR 대조
- 3 ECOUNT 수신문서 확인: 링크의 원본 발주서를 열어 품목·수량·납기·입고지 확인
- 4 고객사 발주 사이트 확인: 메일의 PO로 사이트 상세를 조회해 품목·발주량·납기·납품장소 확인

##### 5.1.2. 발주 DB 처리
- 우선 발주 데이터모델을 정의해서 만든다.
- 구성: 기본 발주내용(업체,품목,수량,납기일,납품처) + 오더시트상태값(오더시트에 입력이 되어있는지)

##### 5.1.3. 오더시트/재고표 비교
- legacy_project_architecture 파일의 9,10 항목 진행하여 5.1.2에 해당하는 오더시트상태값을 갱신한다.
- 재고가 있고, 오더시트입력이 되어있어야지만 오더시트 상태값 true
- 나머지는 false이다

##### 5.1.4. 디코 메시지 발송
- 오더시트상태값에 따라 발송되어야하는 디코채널이 달라짐
- true이면 "오더시트" 채널로 / false면 "발주" 채널로

---

#### 5.2. 오더처리
- 디코 채널은 2개로 구분됨.
    - 오더 (디코 카테고리)
        - rw (디코 채널)
        - si (디코 채널)
        - 완료 (디코 채널)

##### 5.2.1 오더 DB 처리
- rw, si를 데이터베이스 테이블로 따로 관리
- 데이터모델은 codex 기존 풍뎅이관리 프로젝트 기준으로 설계
- 본 단계에서는 메일 내용에 맞춰 rw, si 데이터베이스 CRUD 등을 수행
- rw, si 데이터베이스 활용은 지금단계에서는 사용 x. 추후에 활용 예정.

##### 5.2.2 AI 카드 내용 작성
- 오더 프롬프트에 맞게 ai카드 내용 작성
- 오더 프롬프트는 기존 풍뎅이관리 프로젝트에서 필요한 내용들을 가져온다.
- 크게 진행중/완료로 나뉜다. 진행중인 항목은 rw/si에 맞게 분류하여 해당 채널로 전송 / 완료된 항목은 완료 채널로 전송

##### 5.2.3 디코 메시지 발송

---

#### 5.3. 샘플자료견적 처리
- 디코 채널은 2개로 구분됨.
    - 샘플자료견적 (디코 카테고리)
        - 진행중 (디코 채널)
        - 완료 (디코 채널)

##### 5.3.1 AI로 메시지 발송 / 삭제인지 분류
- 샘플자료견적 프롬프트는 기존 풍뎅이관리 프로젝트에서 필요한 내용들을 가져온다.
- 샘플자료견적 프롬프트를 보고 신규 요청인지, 기존 요청 처리인지를 판단한다.

##### 5.3.2 신규 요청
- 신규 요청인 경우, gmail id와 함께 디스코드 메시지 데이터를 만들고  진행중 채널로 발송

##### 5.3.3 기존 요청에 대한 완료처리
- 기존 요청에 대한 완료처리는 기존 메일에 대한 회신으로 올 것이다.
- 기존 요청 gmail id를 사용한 기존 디코 메시지를 찾아서 삭제하고, 갱신된 내용을 같은 채널로 디코 메시지로 발송한다.
- 샘플/자료/견적으로 내용은 구성됨 (3개다 필수는 아님. 부분적으로 요청 가능)
- 완료처리는 부분적으로 될 수 있음. 샘플 부분처리, 자료 부분처리, 견적 부분처리로 나눠서 메일이 올 수 있음.
- 모두 완료처리가 안된 경우, 기존 디코 메시지를 찾아서 삭제하고, 같은 채널로 갱신된 내용으로 발송 (~~A~~ 표기로 업데이트 된 부분 표기)
- 요청에 대한 항목 모두 완료처리가 된 경우, 기존 디코 메시지를 찾아서 삭제하고, 완료 채널로 디코 메시지를 발송한다.

---

#### 5.4. 풍림자료요청 처리
- 디코 채널은 4개로 구분됨.
    - 풍림자료요청 (디코 카테고리)
        - nikko (디코 채널)
        - seiwa (디코 채널)
        - 기타 (디코 채널)
        - 완료 (디코 채널)

##### 5.4.1 AI로 메시지 발송 / 삭제인지 분류
- 샘플자료견적 프롬프트는 기존 풍뎅이관리 프로젝트에서 필요한 내용들을 가져온다.
- 풍림자료요청 카테고리 프롬프트를 보고 신규 요청인지, 기존 요청 처리인지를 판단한다.

##### 5.4.2 신규 요청
- 신규 요청인 경우, gmail id와 함께 디스코드 메시지 데이터를 만들고  채널로 발송
- 업체명이 Nikko, Seiwa 2개 체널에 맞게 발송한다.
- 2개에도 해당되지 않는 경우, 기타 채널로 발송한다.

##### 5.4.3 기존 요청에 대한 완료처리
- 기존 요청에 대한 완료처리는 기존 메일에 대한 회신으로 올 것이다.
- 기존 요청 gmail id를 사용한 기존 디코 메시지를 찾아서 삭제하고, 갱신된 내용을 같은 채널로 디코 메시지로 발송한다.
- 완료처리는 부분적으로 될 수 있음. A,B,C로 신규 요청이 있을 경우, A 부분처리, B/C 부분처리로 나눠서 메일이 올 수 있음.
- 모두 완료처리가 안된 경우, 기존 디코 메시지를 찾아서 삭제하고, 같은 채널로 갱신된 내용으로 발송 (~~A~~ 표기로 업데이트 된 부분 표기)
- A,B,C 모두 완료처리가 된 경우, 기존 디코 메시지를 찾아서 삭제하고,완료 채널로 디코 메시지를 발송한다.

---

#### 5.5. 사내업무 처리
- 디코 채널은 1개로 구분됨.
    - 기타 (디코 카테고리)
        - 사내업무 (디코 채널)

##### 5.5.1 AI 카드 내용 작성
- 사내업무 프롬프트는 기존 풍뎅이관리 프로젝트에서 필요한 내용들을 가져온다.
- 사내업무 카테고리 프롬프트에 맞게 ai카드 내용 작성

##### 5.5.2 디코 메시지 발송
- 사내업무 채널로 디코 메시지 발송

#### 5.6. 해외업무 처리
- 디코 채널은 1개로 구분됨.
    - 기타 (디코 카테고리)
        - 해외업무 (디코 채널)

##### 5.6.1 AI 카드 내용 작성
- 해외업무 프롬프트는 기존 풍뎅이관리 프로젝트에서 필요한 내용들을 가져온다.
- 해외업무 카테고리 프롬프트에 맞게 ai카드 내용 작성

##### 5.6.2 디코 메시지 발송
- 해외업무 채널로 디코 메시지 발송

#### 5.7. 보류 처리
- 디코 채널은 1개로 구분됨.
    - 기타 (디코 카테고리)
        - 보류 (디코 채널)

##### 5.7.1 AI 카드 내용 작성
- 보류 프롬프트는 기존 풍뎅이관리 프로젝트에서 필요한 내용들을 가져온다.
- 보류 카테고리 프롬프트에 맞게 ai카드 내용 작성

##### 5.7.2 디코 메시지 발송
- 보류 채널로 디코 메시지 발송

---

## 오더시트/재고표 비교 workflow 흐름
1시간에 한번씩만 진행하여 발주 오더시트상태값을 갱신한다.
오더시트/재고표 보는 코어 로직은 5.1.3단계와 같음

### 1. 진행중인 발주 목록 추출
발주 데이터베이스에 저장된 모든 발주에 대해서 추출하여 진행한다.

### 2. 각 발주별 오더시트/재고표 확인
5.1.3과 동일

### 3. 조건에 따라 상태값 변경
5.1.3과 동일

### 4. 디스코드 메시지 반영
5.1.4과 동일